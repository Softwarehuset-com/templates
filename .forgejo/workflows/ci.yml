name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-sample:
    uses: ./.forgejo/workflows/test-dotnet.yml
    # Smart defaults handle everything:
    # - auto-detects samples/dotnet-api/SampleApi.sln
    # - auto-detects docker-compose.yml in samples/dotnet-api/
    # - installs .NET 9.0
    # - submodules: true
