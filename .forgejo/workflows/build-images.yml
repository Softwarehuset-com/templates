name: build-images

on:
  workflow_call:
    inputs:
      registry:
        description: "Container registry"
        type: string
        default: "code.core.ci/softwarehuset"
      images:
        description: "Images to build (one per line: dockerfile|image-name|context)"
        type: string
        required: true
      tag:
        description: "Image tag (empty = git sha short)"
        type: string
        default: ""
      push:
        description: "Push images to registry"
        type: boolean
        default: true
      submodules:
        description: "Checkout with submodules"
        type: boolean
        default: false
    secrets:
      forgejo_token:
        description: "Token for private submodule checkout"
        required: false
      REGISTRY_USERNAME:
        description: "Registry username for docker login"
        required: true
      REGISTRY_PASSWORD:
        description: "Registry password/token for docker login"
        required: true

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: ${{ inputs.submodules }}
          # Fall back to the runner-injected token when the caller doesn't
          # pass a custom forgejo_token. Without the fallback, an unset
          # secret resolves to an empty string and checkout@v4 rejects it
          # with "Input required and not supplied: token".
          token: ${{ secrets.forgejo_token || github.token }}

      - name: Login to registry
        if: ${{ inputs.push }}
        run: |
          echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login "${{ inputs.registry }}" \
            -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin

      - name: Build and push images
        run: |
          TAG="${{ inputs.tag }}"
          if [ -z "$TAG" ]; then
            TAG=$(echo "${{ github.sha }}" | head -c 7)
          fi

          echo "${{ inputs.images }}" | while IFS='|' read -r dockerfile image_name context; do
            # Skip empty lines
            [ -z "$dockerfile" ] && continue

            # Trim whitespace
            dockerfile=$(echo "$dockerfile" | xargs)
            image_name=$(echo "$image_name" | xargs)
            context=$(echo "${context:-.}" | xargs)

            FULL_IMAGE="${{ inputs.registry }}/${image_name}:${TAG}"
            LATEST_IMAGE="${{ inputs.registry }}/${image_name}:latest"

            echo "Building $FULL_IMAGE from $dockerfile (context: $context)"
            docker build -f "$dockerfile" -t "$FULL_IMAGE" -t "$LATEST_IMAGE" "$context"

            if [ "${{ inputs.push }}" = "true" ]; then
              docker push "$FULL_IMAGE"
              docker push "$LATEST_IMAGE"
            fi
          done
